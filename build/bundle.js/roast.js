// Generated by CoffeeScript 1.8.0
(function() {
  var NOOP_ACTION, Roast;

  Roast = {};

  Roast.elements = {
    div: function(props, children) {}
  };

  NOOP_ACTION = ['NOOP'];

  Roast.start = function(initialModel, reduce, view) {
    var model, send;
    model = initialModel;
    send = function(action) {
      var dom;
      model = reduce(action);
      return dom = view(send, model);
    };
    return send(NOOP_ACTION);
  };

  window.Roast = Roast;

}).call(this);
